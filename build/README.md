This folder contains build scripts/config that are required to run `npm run build`.

Notes:
- Keep only scripts/config here (e.g. `*.mjs`, PostCSS config).
- Put template archives and other third-party source assets under `vendor/` (ignored by git).
